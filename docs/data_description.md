# Data Fusion Contest 2026 — Подробное описание данных (Задача 1 «Страж»)

## Периоды
1. **Pre-train** (2023-10-01 — 2024-09-30)
   - Только данные операций.
2. **Train** (2024-10-01 — 2025-05-31)
   - Данные операций + разметка целевой переменной.
3. **Pre-test** (2025-06-01 — 2025-08-09)
   - История тестовых данных (без разметки).
4. **Test** (2025-06-01 — 2025-08-09)
   - Финальный день операций клиента для предсказания.

## Разбиение файлов
Для удобства pre-train и train разбиты на 3 части по группам `customer_id`.
Записи отсортированы по `customer_id` и времени `event_dttm`.

## Ключевые сущности и таргет
- `customer_id` — id клиента банка
- `event_id` — id операции
- `target`:
  - `1` — неподтвержденная операция (🔴, целевой класс)
  - `0` — подтвержденная клиентом подозрительная операция (🟡)
  - Остальные операции без обратной связи считаются (🟢)

## Материалы и размеры
- 1.2MB — разметка Train
- 21.9MB — пример базового решения

### Pre-train (3 части)
- 625.2MB — 1/3 клиентов
- 622.8MB — 2/3 клиентов
- 622.8MB — 3/3 клиентов

### Train (3 части)
- 688.1MB — 1/3 клиентов
- 686.5MB — 2/3 клиентов
- 688.1MB — 3/3 клиентов

### Pre-test / Test
- 338.9MB — pre-test операции (до последнего дня клиентов)
- 17.2MB — test операции (последний день клиентов)

## Глоссарий полей операций
- `customer_id` — id клиента
- `event_id` — id операции
- `event_dttm` — дата/время операции
- `event_type_nm` — тип операции
- `event_desc` — закодированное описание операции
- `channel_indicator_type` — канал совершения операции
- `channel_indicator_subtype` — подтип канала
- `operaton_amt` — сумма операции в рублях
- `currency_iso_cd` — валюта операции
- `mcc_code` — группа MCC (merchant_category_code)
- `pos_cd` — закодированное значение POS condition code
- `accept_language` — язык заголовка HTTP запроса
- `browser_language` — язык браузера
- `timezone` — часовой пояс
- `session_id` — идентификатор сессии
- `operating_system_type` — закодированная ОС
- `battery` — заряд устройства
- `device_system_version` — версия ОС
- `screen_size` — разрешение экрана
- `developer_tools` — флаг настроек разработчика
- `phone_voip_call_state` — флаг VoIP-звонка во время операции
- `web_rdp_connection` — флаг удаленного управления устройством
- `compromised` — наличие root-доступа на устройстве
